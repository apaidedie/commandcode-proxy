services:
  commandcode-proxy:
    image: al1ya/commandcode-proxy:latest
    container_name: commandcode-proxy
    restart: unless-stopped
    ports:
      - '3050:3050'
    volumes:
      - ./logs:/app/logs
